
#Machine Energy Usage Config. This config is synced from server to client.
[usage]
	#Energy per operation tick (Joules).
	enrichmentChamber = "25"
	#Energy per operation tick (Joules).
	osmiumCompressor = "25"
	#Energy per operation tick (Joules).
	combiner = "25"
	#Energy per operation tick (Joules).
	crusher = "25"
	#Energy per operation tick (Joules).
	metallurgicInfuser = "25"
	#Energy per operation tick (Joules).
	purificationChamber = "25"
	#Energy per operation tick (Joules).
	energizedSmelter = "25"
	#Energy per operation tick (Joules).
	digitalMiner = "75000"
	#Energy per operation tick (Joules).
	electricPump = "25"
	#Energy that can be transferred at once per charge operation (Joules).
	chargePad = "1024000"
	#Energy per operation tick (Joules).
	rotaryCondensentrator = "10"
	#Energy per operation tick (Joules).
	oxidationChamber = "25"
	#Energy per operation tick (Joules).
	chemicalInfuser = "10"
	#Energy per operation tick (Joules).
	chemicalInjectionChamber = "400"
	#Energy per operation tick (Joules).
	precisionSawmill = "25"
	#Energy per operation tick (Joules).
	chemicalDissolutionChamber = "25"
	#Energy per operation tick (Joules).
	chemicalWasher = "25"
	#Energy per operation tick (Joules).
	chemicalCrystallizer = "25"
	#Energy per operation tick (Joules).
	seismicVibrator = "25"
	#Energy per operation tick (Joules).
	pressurizedReactionBase = "5"
	#Energy per operation tick (Joules).
	fluidicPlenisher = "25"
	#Energy per operation tick (Joules).
	laser = "10000"
	#Energy per operation tick (Joules).
	formulaicAssemblicator = "25"
	#Energy per operation tick (Joules).
	modificationStation = "25"
	#Energy per operation tick (Joules).
	isotopicCentrifuge = "10"
	#Energy per operation tick (Joules).
	nutritionalLiquifier = "25"
	#Energy per operation tick (Joules).
	antiprotonicNucleosynthesizer = "10000"
	#Energy per operation tick (Joules).
	pigmentExtractor = "25"
	#Energy per operation tick (Joules).
	pigmentMixer = "25"
	#Energy per operation tick (Joules).
	paintingMachine = "25"
	#Energy per chunk per tick (Joules).
	dimensionalStabilizer = "5000"

	#Teleporter
	[usage.teleporter]
		#Base Joules cost for a teleportation.
		teleporterBase = "1000"
		#Joules per unit of distance travelled during teleportation - sqrt(xDiff^2 + yDiff^2 + zDiff^2).
		teleporterDistance = "10"
		#Flat additional cost for interdimensional teleportation. Distance is still taken into account minimizing energy cost based on dimension scales.
		teleporterDimensionPenalty = "10000"

